import React from "react";
import styled from "styled-components";

const ProfileForm = ({ me }) => {
  return <MainDiv>{/* <>{me.name}</div> */}</MainDiv>;
};

export default ProfileForm;

const MainDiv = styled.div`
  height: 300px;
`;
